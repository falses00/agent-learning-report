# Agent 教学平台维护规范

更新日期：2026-08-20
适用范围：课程 Markdown、前端数据、可执行实验、评测集、外部来源、GitHub Pages 发布物。

## 1. 维护目标

本项目不是一次性资料合集。维护必须保证：

1. 学习者始终有一条明确的工程主线和可运行下一步。
2. 主线结论由当前官方文档、标准、原始论文、上游源码或项目实验支撑。
3. 历史资料保留原理价值，但不会伪装成当前 API、生产默认或招聘事实。
4. 每次内容或功能变更都有来源、版本、测试、浏览器证据和回滚方式。
5. 外部项目的 star、营销数字和单一 benchmark 不成为生产采用依据。

## 2. 内容所有权

| 资产 | 规范入口 | 维护责任 |
|---|---|---|
| 课程主线 | `agent-runtime-gateway/00-课程总览/工程实战主线-v2.md` | 阶段顺序、前置、产物、门禁一致 |
| RAG 主线 | `agent-runtime-gateway/07-RAG问题诊断与优化/RAG教学路线总览.md` | 来源、失败切片、强基线、复杂方法边界 |
| RAG 方法与资料状态 | `data/rag_lab.js` | 方法成熟度、source lifecycle、项目活跃度 |
| Vibe Coding | `agent-runtime-gateway/00-课程总览/Vibe-Coding最佳实践-2026.md` | 当前工具实践、证据边界、安全责任 |
| 面试岗位证据 | `data/interview_lab.js`、`agent-runtime-gateway/00-课程总览/大厂Agent岗位面试知识地图-2026.md` | 官方岗位状态、能力交集、题目边界、复核日期 |
| 可执行实验 | `agent-runtime-gateway/labs/**`、`20-源码`、`21-测试` | 命令可运行、预期与实际一致、失败路径可复现 |
| 评测集 | `agent-runtime-gateway/22-评测集` | schema、版本、critical blocker、覆盖与泄漏审计 |
| 阅读器内容 | `data/build_chapters.mjs` -> `data/chapters.js` | 源文件存在、生成物同步、链接可解析 |
| 公开站点 | `index.html`、GitHub Pages | 桌面/移动端可读、键盘可用、无 console error |

新增同类资料前先扩展规范入口，避免再建立平行“完整版”“最终版”文档。

## 3. 来源分级

| 状态 | 定义 | 能进入哪里 | 复核频率 |
|---|---|---|---|
| 当前采用 | 当前官方文档/标准、活跃上游、近两年高质量原始研究，并与课程结论直接相关 | 主线、实验候选、前端默认 | 30–90 天 |
| 持续观察 | 新预印本、快速变化项目、自动评测框架、尚未在本项目复现的方法 | 研究候选、未来走向、对照实验 | 30–60 天 |
| 历史基础 | 奠基论文、旧版机制、仍有原理价值但不代表当前生产实践 | 原理和演进背景 | 180 天 |
| 已退出主线 | API 迁移、项目停更、链接失效、被更强来源替代或会误导学习顺序 | 淘汰记录，不再作为默认链接 | 只在替代项变化时复核 |

### 主张与证据

- 官方文档说明当前 API、配置和迁移，不证明方案在本项目更好。
- 原始论文说明方法、实验设置和论文结果，不证明生产 SLA。
- 固定 commit 源码说明机制真实存在，不证明维护质量、许可证适配或安全性。
- benchmark 说明固定数据和预算下的表现，不证明跨领域通用最优。
- 只有本项目的版本化 eval、trace、成本和对抗结果，才能支持项目采用结论。

### 面试证据 A / B / C

| 等级 | 允许来源 | 能证明什么 | 禁止外推 |
|---|---|---|---|
| A · 官方岗位 | 企业官方 careers/jobs 与官方招聘说明 | 核验日的职责、要求、公开流程说明 | 不代表实际题目、频率、职级或长期不变 |
| B · 上游工程 | GitHub 上游 README、release、源码目录和测试 | 某个实现与版本真实存在、值得 code reading | 不代表该实现安全、生产最优或招聘高频 |
| C · 社区经验 | 带日期、上下文、可读正文的 Linux DO、牛客等公开帖子 | 候选训练假设、项目追问和 failure drill | 不得写成公司官方流程、真题、标准答案或频率统计 |

搜索摘要、转载题库、课程营销文和无法打开的帖子不能进入 A / B / C 当前集。C 类必须同时显示日期、偏差和安全转化的训练动作。

## 4. 资料准入与淘汰

### 准入清单

- [ ] 主线来源是官方文档、标准、原始论文或上游仓库；面试社区样本只能进入 C 类观察区。
- [ ] 记录标题、URL、发布日期/版本、访问日期和对应课程主张。
- [ ] 说明它能证明什么、不能证明什么。
- [ ] 易变源码固定 commit；当前 API 保留复核日期。
- [ ] 与既有资料不重复，或明确替代旧来源。
- [ ] 课程采用点能落到实验、评测、案例或决策规则。
- [ ] 不含 secret、私有数据、未经许可的大段转载。

### 降级或退出条件

出现任一情况时，把资料从“当前采用”降为“观察/历史/退出”：

1. 官方入口迁移到 legacy/classic/deprecated，主线 API 已改变。
2. 上游 12 个月无有效活动，且缺少维护或安全说明。
3. 论文方法只有作者数据平均分，没有可复现代码或失败边界。
4. 新证据推翻课程中的普遍化表述。
5. 链接失效、许可证不兼容、项目归档或存在未处理高风险公告。
6. 资料只重复营销主张，不能支持新的教学决策。
7. 旧路线会暗示高级方法是必经步骤，破坏“失败切片 -> 强基线 -> 消融”的主线。

淘汰不是静默删除：在 registry 记录原因、替代项和日期。奠基论文通常进入“历史基础”，而不是被删除。

## 5. 维护节奏

| 周期 | 要做的事 | 证据 |
|---|---|---|
| 每次修改 | 重建章节、目标检查、完整 quality gate、浏览器冒烟、git diff | 命令输出、截图或浏览器断言 |
| 每两周 | 抽查中国国内官方招聘链接、岗位状态与重复能力信号 | `reviewedAt`、`reviewDueAt`、退出记录 |
| 每月 | 抽查国际岗位、GitHub 上游最后活动/release、安全公告、社区样本可访问性与 source lifecycle | `reviewedAt`、维护提交 |
| 每季度 | 复核课程主线、招聘能力方向、重复文档、移动端和无障碍 | 审计记录、路线变更说明 |
| 事件触发 | API deprecation、模型/框架大版本、安全事件、关键论文或 benchmark | 影响分析、迁移/回滚计划 |

招聘页面只用于校准能力方向。中国国内官方岗位最长 14 天复核一次，国际岗位最长 30 天；不能把单个职位描述写成永久课程事实。

面试岗位事实只接受公司官方招聘页与官方面试说明。GitHub 上游项目进入 B 类工程深挖；Linux DO、牛客等社区经验进入 C 类训练线索，必须展示日期和偏差，不能标成官方原题、公司固定流程或频率。官方岗位关闭后应在对应复核窗口退出当前数据集，保留历史决策记录；至少两个独立公司的 A 类重复信号才能升级为主线能力。

## 6. 标准更新流程

1. **Survey**：读本规范、相关主线、数据源、测试、`git status`；列出用户修改，禁止覆盖。
2. **Research**：技术事实只查一手来源；记录发布日期、版本、证据边界和剩余未知。
3. **Decide**：给来源标记 lifecycle；决定新增、替代、降级或退出，不直接堆到资料列表。
4. **Edit**：先改 canonical Markdown 和结构化数据，再改前端；同类结论只保留一个主入口。
5. **Generate**：从 Markdown 重建 `data/chapters.js`，禁止手工编辑生成文件。
6. **Target verify**：运行受影响的内容检查、实验测试和一个失败/对抗探针。
7. **Full verify**：运行完整 quality gate；前端用真实浏览器检查桌面、390px 和 320px。
8. **Review**：检查 diff、外部链接、测试是否弱化、无关文件、生成物和 source status。
9. **Release**：提交、推送并核验公开站点；记录 commit、日期和残余风险。
10. **Learn**：真实事故、低分反馈和维护失误先进入 regression/规范，再扩展功能。

## 7. 维护命令

在项目根目录运行：

```powershell
node data/build_chapters.mjs
node data/check_rag_content.mjs
node data/check_interview_content.mjs
node data/check_reader_content.mjs
python scripts/run_quality_gate.py
git diff --check
```

S3 RAG 目标回归：

```powershell
cd "agent-runtime-gateway\20-源码"
python -m pytest ..\21-测试\test_rag.py ..\21-测试\test_evals.py ..\21-测试\test_rag_diagnostics.py -q
python -m agent_course.cli eval ..\22-评测集\s3-rag-baseline.json
python -m agent_course.cli rag-diagnostic-eval ..\22-评测集\rag-diagnostic-baseline.json
```

任何命令无法运行时，发布说明必须写明阻塞原因和残余风险，不能用“预计通过”替代真实结果。

## 8. 前端维护规则

- 第一屏直接进入可用学习平台，不做营销式 landing page。
- 主线使用连续章节、表格和步骤带；卡片只用于真正重复项目或独立工具。
- 每个专项最多保留一个清晰主导航；移动端可以横向滚动，但文字不能溢出。
- 长文提供章节目录、阅读进度和预计时长；正文宽度保持可阅读行长。
- 交互控件使用可见 label、键盘焦点、`aria-live` 和明确空状态。
- 新筛选必须有“全部”恢复路径，刷新后不能留下不可达状态。
- 320px、390px、桌面至少各检查一次；页面不能横向溢出、遮挡或出现空白交互区。
- 减少动态装饰，尊重 `prefers-reduced-motion`。

## 9. 发布阻塞规则

以下任一项失败就不能发布：

- canonical Markdown 与 `data/chapters.js` 不同步。
- 内容 gate 或实验测试失败。
- 外部来源没有 lifecycle 或过时入口仍被标为当前默认。
- RAG 越权、注入、陈旧关键证据或伪引用 case 失败。
- 前端有 uncaught exception、主要交互不可键盘操作、320px 横向溢出。
- 修改覆盖了用户未提交工作，或提交混入无关大文件、secret、缓存和构建产物。

## 10. 维护记录模板

```text
日期：YYYY-MM-DD
范围：课程 / RAG / Vibe Coding / 前端 / 实验 / 评测
触发：定期 / API 迁移 / 安全事件 / 学习反馈 / 新证据
新增或更新的一手来源：
降级或退出的资料及原因：
改变的教学结论：
运行的命令与实际结果：
浏览器视口与交互证据：
残余风险与下次复核日期：
commit / public URL：
```

本规范本身每季度复核一次。维护流程变更必须同步 quality gate 或项目文档，不能只留在聊天记录中。
